Django>=4.0,<5.0
gunicorn>=20.1,<21.0
psycopg2>=2.9,<3.0  # If you're using PostgreSQL (adjust if you're using another DB)
django-environ>=0.8,<1.0  # For managing environment variables (optional but recommended)
